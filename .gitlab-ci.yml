image: docker:latest

services:
    - docker:dind

stages:
    - deploy

docker_build_deploy:
    stage: deploy
    script:
        - echo "$ENV_FILE" > server/.env
        - docker build -t react_demo .
        - docker run -d -p 5000:5000 react_demo
    only:
        refs:
            - master
    